# Minimal runtime requirements for DeepSeek-OCR API service.
# Pip resolves transitive deps (CUDA libs, triton, pydantic, jinja2, etc.) automatically.
#
# flash_attn note: the prebuilt PyPI wheel ships with a C++ ABI that does not
# match the official PyPI torch 2.6 wheel (cxx11abi=FALSE). To build from
# source against this torch, install with:
#   FLASH_ATTENTION_FORCE_BUILD=TRUE _GLIBCXX_USE_CXX11_ABI=0 \
#     pip install --no-build-isolation flash_attn==2.8.3

# --- ML stack ---
torch==2.6.0
torchvision==0.21.0
transformers==4.57.6
vllm==0.8.4
flash_attn==2.8.3

# --- model utilities ---
huggingface_hub==0.36.2
einops==0.8.2
addict==2.4.0
easydict==1.13
tqdm==4.67.3

# --- web service ---
fastapi==0.135.2
uvicorn==0.42.0
python-multipart==0.0.22
requests==2.32.3

# --- image / PDF handling ---
numpy==1.26.4
pillow==12.1.1
PyMuPDF==1.27.2.2
img2pdf==0.6.3
